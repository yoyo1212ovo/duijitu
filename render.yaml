services:
  - type: web
    name: duijitu
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /tmp/data
      - key: UPLOADS_DIR
        value: /tmp/uploads
      - key: JWT_SECRET
        generateValue: true
